Two bundled faces, both under the SIL Open Font License 1.1, whose full text sits beside each:

  InterVariable.woff2   Inter, by The Inter Project Authors     -> Inter-OFL.txt
  SpaceGrotesk.woff2    Space Grotesk, by Florian Karsten and   -> SpaceGrotesk-OFL.txt
                        The Space Grotesk Project Authors
                        (latin subset, variable 300-700, from Google Fonts)

Vite fingerprints and inlines them at build time, so the app fetches no font at runtime and works offline.
